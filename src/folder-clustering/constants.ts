import type { FolderVirtualLinksSettings } from "./types";

export const PLUGIN_ID = "folder-virtual-links";
export const DEFAULT_SETTINGS: FolderVirtualLinksSettings = {
  topologyDegree: 3
};

export const TOPOLOGY_ATTEMPTS = 512;
